<!DOCTYPE html>
<html>

<head>
    <% include ../static/partials/head.ejs %>
</head>

<body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
        <h1>Edit Flair</h1>

        <form action="/topics/<%= flair.topicId %>/flair/<%= flair.id %>/update" method="POST">
            <div class="form-group">
                <label for="name">Flair Name</label>
                <input value="<%= flair.name %>" type="text" class="form-control" name="name"
                    aria-describedby="titleHelp" placeholder="Enter Name">
            </div>
            <div class="form-group">
                <label for="color">Flair Color</label>
                <input value="<%= flair.color %>" type="text" class="form-control" name="color"
                    aria-describedby="titleHelp" placeholder="Choose Color (text-info, text-primary, text-danger)">
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
        </form>
    </main>

    <% include ../static/partials/baseScripts.ejs %>
</body>

</html>